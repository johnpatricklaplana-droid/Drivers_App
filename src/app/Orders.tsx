import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Orders () {
    return (
        <SafeAreaView>
            <Text style={{ color: 'black' }}>Order Now</Text>
        </SafeAreaView>
    );
}